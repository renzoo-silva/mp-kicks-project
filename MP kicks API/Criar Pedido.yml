info:
  name: Criar Pedido
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:3000/pedidos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
