info:
  name: Cadastrar Cliente
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3000/clientes/register
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
