info:
  name: Login Cliente
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3000/clientes/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
